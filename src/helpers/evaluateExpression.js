// given a space-separated mathmatic expression, perform all calculations from left to right

const evaluateExpression = (exp) => {
    let segmentedOpAndVals = exp.split(' ')

    let a;
    let b;

    
}